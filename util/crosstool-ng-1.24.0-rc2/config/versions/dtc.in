#
# DO NOT EDIT! This file is automatically generated.
#

# The component directory name
config DTC_DIR_NAME
    string
    default "dtc"



config DTC_PKG_NAME
    string
    default "dtc"

choice
    bool "Source of dtc"

config DTC_SRC_RELEASE
    bool "Released tarball"
    help
      Download a released tarball.

config DTC_SRC_DEVEL
    bool "Vendor/custom repository"
    depends on EXPERIMENTAL
    help
      Check out from a repository.
      Default is the vendor repository at git://git.kernel.org/pub/scm/utils/dtc/dtc.git

if DTC_SRC_DEVEL

choice
    bool "VCS type"
    default DTC_DEVEL_VCS_git
    help
      Version control system from which the sources will be checked out.
      The default value points to the development repository for dtc.

config DTC_DEVEL_VCS_git
    bool "Git"

config DTC_DEVEL_VCS_svn
    bool "Subversion"

config DTC_DEVEL_VCS_hg
    bool "Mercurial"

config DTC_DEVEL_VCS_cvs
    bool "CVS"

endchoice

config DTC_DEVEL_VCS
    string
    default "git" if DTC_DEVEL_VCS_git
    default "svn" if DTC_DEVEL_VCS_svn
    default "hg" if DTC_DEVEL_VCS_hg
    default "cvs" if DTC_DEVEL_VCS_cvs

config DTC_DEVEL_URL
    string "Repository URL"
    default "git://git.kernel.org/pub/scm/utils/dtc/dtc.git"
    help
      Repository URL.

      For CVS, enter both the value of CVS root and the module name, separated
      by a space.

config DTC_DEVEL_BRANCH
    string "Branch/tag to check out"
    default ""
    help
      Git/CVS: branch/tag to be checked out
      Subversion: directories to append to the repository URL (i.e. branch or tag)
      Mercurial: official guide recommends using separate repositories to maintain
      stable branches. You likely need to change the repository URL, rather than
      enter anything here.

config DTC_DEVEL_REVISION
    string "Revision/changeset"
    default ""
    help
      Commit ID or revision ID to check out.
      Git: enter the commit ID to check out a commit.
      CVS: enter the date in "YYYY/MM/DD HH:MM:SS" format (UTC) to check out certain date.
      Subversion: enter the revision.

config DTC_DEVEL_SUBDIR
    string "Subdirectory in the repository"
    default ""
    help
      Some projects produce releases not from the top-level directory in the
      repository, but rather from some subdirectory. If it is the case,
      specify this subdirectory here.

config DTC_DEVEL_BOOTSTRAP
    string "Bootstrap command"
    default ""
    help
      Command to run after checking out. Some projects don't store the generated
      files like configure script in the repository; building out of a checked out
      working copy thus requires some extra steps. Separate multiple shell commands
      with &&.

endif

config DTC_SRC_CUSTOM
    bool "Custom location"
    depends on EXPERIMENTAL
    help
      Custom directory or tarball.

if DTC_SRC_CUSTOM

config DTC_CUSTOM_LOCATION
    string "Custom source location"
    help
      Path to the directory or tarball with the sources.

endif

endchoice

if EXPERIMENTAL
choice
    bool "dtc patches origin"
    default DTC_PATCH_GLOBAL

config DTC_PATCH_GLOBAL
    bool "Per global policy"

config DTC_PATCH_BUNDLED
    bool "Bundled only"

config DTC_PATCH_LOCAL
    bool "Local only"
    select PATCH_USE_LOCAL

config DTC_PATCH_BUNDLED_LOCAL
    bool "Bundled, then local"
    select PATCH_USE_LOCAL

config DTC_PATCH_LOCAL_BUNDLED
    bool "Local, then bundled"
    select PATCH_USE_LOCAL

config DTC_PATCH_NONE
    bool "None"

endchoice
endif

config DTC_PATCH_ORDER
    string
    default "bundled" if DTC_PATCH_BUNDLED
    default "local" if DTC_PATCH_LOCAL
    default "bundled,local" if DTC_PATCH_BUNDLED_LOCAL
    default "local,bundled" if DTC_PATCH_LOCAL_BUNDLED
    default "none" if DTC_PATCH_NONE
    default "global"

choice
    bool "Version of dtc"
    help
      For a released version, select the version of dtc to download
      and build. For sources out of the vendor repository or from a custom
      location, select the version that describes those custom sources.
      Based on this version, crosstool-NG may apply some version-specific
      quirks while building dtc.

config DTC_VERY_NEW
    bool "newer than anything below"
    depends on EXPERIMENTAL
    depends on DTC_SRC_DEVEL || DTC_SRC_CUSTOM

config DTC_V_1_4
    bool "1.4.7"

config DTC_VERY_OLD
    bool "older than anything above"
    depends on OBSOLETE && EXPERIMENTAL
    depends on DTC_SRC_DEVEL || DTC_SRC_CUSTOM

endchoice


config DTC_VERSION
    string
    default "1.4.7" if DTC_V_1_4
    default "new" if DTC_VERY_NEW
    default "old" if DTC_VERY_OLD
    default "unknown"

config DTC_MIRRORS
    string
    default "$(CT_Mirrors kernel.org dtc)"

config DTC_ARCHIVE_FILENAME
    string
    default "@{pkg_name}-@{version}"

config DTC_ARCHIVE_DIRNAME
    string
    default "@{pkg_name}-@{version}"

config DTC_ARCHIVE_FORMATS
    string
    default ".tar.xz .tar.gz"

config DTC_SIGNATURE_FORMAT
    string
    default "unpacked/.sign"




